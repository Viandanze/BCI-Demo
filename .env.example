# NeuroDecode environment variables
# Copy to `.env` and fill in your own values.
# NEVER commit the real `.env` — keep credentials out of version control.

# --- Coze agent backend ("--backend coze") ---
# Deploy your own Coze Coding agent relay (see README -> "Run with Coze agent").
COZE_AGENT_DOMAIN=
COZE_PROJECT_ID=
COZE_API_TOKEN=

# Phase 1 additional requirements (on top of existing NeuroDecode deps)
flask>=3.0
numpy>=1.24
scipy>=1.11
brainflow>=5.2
pyyaml>=6.0

# HTTP client used by the ollama / api / coze LLM backends
requests>=2.28

# Optional: for real decoder
# torch>=2.0
